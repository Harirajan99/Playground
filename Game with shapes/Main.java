#include<iostream>
using namespace std;
int main()
{
  int a,b,d;
  cin>>a>>b;
  d=a*2;
  if(d>b)
    cout<<"circle cannot be inside a square";
  else
    cout<<"circle can be inside a square";
  //Type your code here.
}